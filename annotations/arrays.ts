const carMakers = ['ford', 'toyota'];

const myMaker = carMakers[1].charAt(3);

const makersLen = carMakers.map((car) => car.length);

//flexible types
const importantDates = [new Date(), '2030-10-10'];
